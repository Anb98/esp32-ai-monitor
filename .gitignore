# PlatformIO build artifacts
.pio/

# Go coverage artifacts
backend/coverage
backend/coverage.out

# Local docker-compose host paths (see .env.example)
.env
backend/.env

# Firmware Wi-Fi secrets (see firmware/secrets.ini.example)
firmware/secrets.ini

# Local AI runtime state
.atl/
